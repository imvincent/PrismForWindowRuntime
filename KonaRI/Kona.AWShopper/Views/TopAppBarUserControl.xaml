<UserControl
    x:Class="Kona.AWShopper.Views.TopAppBarUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:infrastructure="using:Kona.Infrastructure" mc:Ignorable="d"
    infrastructure:ViewModelLocator.AutoWireViewModel="true"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <!-- <snippet600> -->
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
        <Button Command="{Binding HomeNavigationCommand}" Margin="10" Style="{StaticResource HouseStyle}" />
        <Button Command="{Binding ShoppingCartNavigationCommand}" Margin="10" Style="{StaticResource CartStyle}" />
    </StackPanel>
    <!-- </snippet600> -->
</UserControl>
